apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pgadmin
  namespace: flux-system
spec:
  interval: 1m0s
  dependsOn:
    - name: cnpg
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./apps/pgadmin
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
