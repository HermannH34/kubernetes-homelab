apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: iterato-db-minotoring
spec:
  selector:
    matchLabels:
      postgresql: iterato-db
  podMetricsEndpoints:
    - port: metrics
