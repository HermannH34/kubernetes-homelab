apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-longhorn
  namespace: uptime-kuma
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
