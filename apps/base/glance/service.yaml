apiVersion: v1
kind: Service
metadata:
  name: glance
  namespace: apps
  labels:
    app: glance
spec:
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    targetPort: web
    protocol: TCP
  selector:
    app: glance
